{
  "name": "num-dsf-allowlist-tooling",
  "version": "0.1.2",
  "private": true,
  "license": "Apache-2.0",
  "description": "Repo-level developer tooling (formatting, hooks, commit linting).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.3.0"
  }
}
