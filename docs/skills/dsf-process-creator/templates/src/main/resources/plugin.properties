# Read by AbstractProcessPluginDefinition at load time.
# This is the ONLY resource-filtered file: Maven substitutes ${project.*} on build.
release-date=${project.build.outputTimestamp}
version=${project.version}
name=${project.artifactId}
title=${project.description}
publisher=${project.organization.name}
publisher-email=info@example.org
